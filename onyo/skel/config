[onyo "history"]
	interactive = tig --follow
	non-interactive = git --no-pager log --follow
[onyo "new"]
	template = empty
[onyo "assets"]
	filename = "{type}_{make}_{model}.{serial}"
[onyo "repo"]
	version = 1
